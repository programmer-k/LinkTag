spring.datasource.url=jdbc:mysql://localhost:3306/linktag
spring.datasource.username=root
# The password is set by the systemd service's environment variable for security.
# Uncomment this if you want to run without systemd.
# spring.datasource.password=your-secret-password
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update

spring.sql.init.mode=never
